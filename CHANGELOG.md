# 更新日志

## 2026-08-29

### 新增

- 每条说说增加独立详情页与稳定永久链接，时间流继续保留。
- 拉取请求自动检查构建、主要页面、搜索、主题切换和明显的无障碍问题，并在 Firefox 与 WebKit 中运行轻量冒烟测试。
- `main` 分支禁止删除和强制推送；源码、配置、依赖与 CI 改动必须通过检查后合并，纯内容发布保留作者直接发布通道。

### 改进

- 说说折叠预览改用最多 80 个可见字符的纯文本摘要，图片按数量提示，展开后显示完整正文。
- 构建时校验站点网址、邮箱、GitHub 地址、可见文字、页脚占位符和本地资源，避免错误配置上线。
- Markdown 图片默认延迟加载；首页只加载当前主题使用的主视觉图片。
- 无技术文章时显示空归档并隐藏搜索；不再生成占位搜索结果。
- 数学公式错误会阻止发布；404 页面不再进入搜索引擎索引。
- 正文链接恢复淡色下划线，并增加核心页面的无障碍检查。

### 不兼容变更

- 说说正式链接由 `/shuoshuo/#<稳定ID>` 改为 `/shuoshuo/<稳定ID>/`，旧锚点链接不再兼容。
