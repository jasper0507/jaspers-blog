# 搜索装配收成一个 module

搜索修改曾要同时改外壳 DOM 与 `global.css` 里的 Pagefind token 和内部 class。把资源装配、是否启用、主题映射和可访问性补丁收进搜索 module；外壳只在主导航放置该 module。Pagefind 是现有 adapter，不引入可替换搜索引擎 port。弹层在 implementation 内放到 `body`，避免成为导航 flex 项。无公开技术文章时 module 不渲染，也不加载第三方资源。
