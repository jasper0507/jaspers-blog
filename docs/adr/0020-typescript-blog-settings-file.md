# 使用 TypeScript 文件承载博客设置

博客设置统一放在一个带中文说明的 `blog.config.ts` 文件中，博客搭建者只需修改其中的值，不需要理解页面源码。相比 YAML 或 JSON，这一选择会露出少量代码符号，但能复用项目已有工具提供填写提示和严格的构建校验，同时无需增加配置解析依赖；未知字段、无效值和冲突组合都必须停止构建，并以中文指出错误位置和修正方向。项目不建设图形化后台或命令行问答入口。
