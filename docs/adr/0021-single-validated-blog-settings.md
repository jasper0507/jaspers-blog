# 全站只使用一份已校验的博客设置

根目录 `blog.config.ts` 是博客搭建者唯一填写的原始设置，内部配置模块统一完成严格校验、可选值补齐和本地资源检查，再把同一份已校验结果提供给 Astro 配置、页面、RSS、SEO 与页脚。实施时将现有设置和个人信息一次性迁入该文件并删除旧入口，不保留兼容层；消费者不得直接读取原始文件、重复解释字段，或保留 Jasper 信息和环境变量作为隐藏回退。配置无效时构建明确失败，避免同一设置在不同输出中产生分歧。
